{
  "name": "poke-battle",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy":"vite build --emptyOutDir && gh-pages -d dist"
  },
  "devDependencies": {
    "stylelint-config-standard": "^22.0.0",
    "gh-pages": "^3.2.3",
    "vite": "^2.6.0"
  }
}
